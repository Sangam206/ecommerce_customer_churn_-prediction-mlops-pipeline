FROM python:3.10-slim
WORKDIR /app
COPY app/ ./app/
COPY models/ ./models/
RUN pip install fastapi uvicorn pandas scikit-learn xgboost joblib prometheus-client evidently
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]